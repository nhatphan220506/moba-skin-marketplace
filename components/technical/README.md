# Văn-owned functional technical components

Build a functional, minimally styled `BlockchainEvidenceTable` that accepts `EvidenceRow[]`. Nhật will apply the final design system and page layout during integration.
