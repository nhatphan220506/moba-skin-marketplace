import { OnchainActionCard } from "@/components/product/OnchainActionCard";
export default async function AuctionPage({ params }: { params: Promise<{ auctionId: string }> }) { const { auctionId } = await params; return <main className="product-main"><header className="product-heading"><div><p className="eyebrow">AUCTION {auctionId}</p><h1>Verdant Sentinel primary sale</h1><p>Approve MockVND, bid from the connected wallet and inspect the resulting escrow evidence.</p></div><div className="workspace-meta"><span>Reserve 100</span><span>Increment 10</span><span>Highest bid 150</span></div></header><section className="action-grid"><OnchainActionCard actionId="approve-primary"/><OnchainActionCard actionId="place-bid"/><OnchainActionCard actionId="withdraw-refund"/><OnchainActionCard actionId="settle-auction"/></section></main>; }

