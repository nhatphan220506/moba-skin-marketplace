import { OnchainActionCard } from "@/components/product/OnchainActionCard";
export function generateStaticParams() { return [{ auctionId: "1" }]; }
export default async function AuctionPage({ params }: { params: Promise<{ auctionId: string }> }) { const { auctionId } = await params; const selected={auctionId}; return <main className="product-main"><header className="product-heading"><div><p className="eyebrow">AUCTION {auctionId}</p><h1>Primary asset auction</h1><p>Approve MockVND, bid from the connected wallet and inspect the resulting escrow evidence.</p></div><div className="workspace-meta"><span>Contract-enforced reserve</span><span>Pull-payment refunds</span><span>On-chain settlement</span></div></header><section className="action-grid"><OnchainActionCard actionId="approve-primary"/><OnchainActionCard actionId="place-bid" initialValues={selected}/><OnchainActionCard actionId="withdraw-refund" initialValues={selected}/><OnchainActionCard actionId="settle-auction" initialValues={selected}/></section></main>; }
