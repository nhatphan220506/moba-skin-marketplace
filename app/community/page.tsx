import { RoleWorkspace } from "@/components/product/RoleWorkspace";
export default function Page() { return <RoleWorkspace eyebrow="FAN WORKSPACE" title="Community Voting" description="Vote from an authorised fan wallet and inspect the public result without confusing one wallet with one verified person." actions={["vote"]}/>; }

