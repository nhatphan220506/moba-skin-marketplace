import Link from "next/link";
import { lifecycle, featuredDesign } from "@/lib/product/catalog";

export default async function DesignPage({ params }: { params: Promise<{ designId: string }> }) {
  const { designId } = await params;
  return <main className="product-main"><header className="design-hero"><div className="design-art"><span>DESIGN #{designId}</span><div className="large-sigil"/><small>LIMITED USAGE ENTITLEMENT</small></div><div><p className="eyebrow">{featuredDesign.status}</p><h1>{featuredDesign.name}</h1><p>{featuredDesign.description}</p><div className="design-actions"><Link className="button-link" href="/auctions/1">View primary auction</Link><Link className="text-link" href="/marketplace/1">View resale listing ↗</Link></div><dl className="design-facts"><div><dt>Creator</dt><dd>{featuredDesign.artist}</dd></div><div><dt>Supported game</dt><dd>{featuredDesign.game}</dd></div><div><dt>Edition</dt><dd>{featuredDesign.edition}</dd></div><div><dt>Votes</dt><dd>{featuredDesign.votes}</dd></div></dl></div></header><section className="detail-grid"><article className="panel"><p className="panel-kicker">MARKET-READY GATE</p><h2>Seven independently enforced conditions</h2><ol className="gate-list">{lifecycle.map((item) => <li key={item}><span>✓</span>{item}<small>Confirmed on-chain</small></li>)}</ol></article><article className="panel"><p className="panel-kicker">RIGHTS BOUNDARY</p><h2>What ownership means</h2><p>This ERC-1155 record represents a limited right to use the selected skin in the supported game under publisher terms. It does not transfer copyright, character ownership or game IP.</p><Link className="text-link" href="/trust">Read trust and legal boundaries ↗</Link></article></section><section className="evidence-preview"><div><p className="eyebrow">AUDIT TRAIL</p><h2>From submission to resale</h2><p>Every business-critical transition is reconstructed from contract receipts; private files are represented by integrity hashes.</p></div><Link className="button-link" href="/evidence">Open blockchain evidence</Link></section></main>;
}

