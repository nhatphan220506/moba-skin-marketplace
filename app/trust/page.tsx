export default function TrustPage() {
  const items = [{title:"What blockchain proves",copy:"Who signed a transaction, what contract state changed, when it was confirmed, current entitlement ownership and how contract-controlled value was allocated."},{title:"What hashes prove",copy:"A later file can be compared with the anchored digest. A hash proves integrity against that reference; it does not prove that the content is true or legally owned."},{title:"What remains private",copy:"KYC, game-account identifiers, full agreements, production assets and detailed review evidence remain off-chain for privacy, confidentiality and cost."},{title:"What the entitlement means",copy:"A limited right to use a supported skin under publisher terms. It is not copyright, character ownership or a right to use the asset in every game."}];
  return <main className="product-main"><header className="product-heading"><div><p className="eyebrow">TRUST MODEL</p><h1>Clear proof without false promises</h1><p>The system uses blockchain where multiple parties need shared enforcement and uses private services where public storage would be harmful.</p></div></header><section className="trust-grid">{items.map((item) => <article className="panel" key={item.title}><h2>{item.title}</h2><p>{item.copy}</p></article>)}</section></main>;
}

