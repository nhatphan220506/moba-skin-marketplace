# Nhật-owned blockchain integration

Later outputs: contract ABIs/addresses, wallet client, typed read/write wrappers, receipt handling and custom-error parsing. Every write action uses the frozen transaction UI states.
